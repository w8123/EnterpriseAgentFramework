apiVersion: apps/v1
kind: Deployment
metadata:
  name: reachai-capability-service
  labels:
    app: reachai-capability-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reachai-capability-service
  template:
    metadata:
      labels:
        app: reachai-capability-service
    spec:
      containers:
        - name: reachai-capability-service
          image: registry.enterprise.com/ai/reachai-capability-service:latest
          ports:
            - containerPort: 18605
          env:
            - name: SERVER_PORT
              value: "18605"
            - name: AI_MYSQL_URL
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: mysql-url
            - name: AI_MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: mysql-user
            - name: AI_MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: mysql-password
            - name: MODEL_SERVICE_URL
              value: http://reachai-model-service:18601
            - name: KNOWLEDGE_SERVICE_URL
              value: http://reachai-knowledge-service:18602
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 18605
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 18605
            initialDelaySeconds: 20
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: reachai-capability-service
spec:
  selector:
    app: reachai-capability-service
  ports:
    - port: 18605
      targetPort: 18605
  type: ClusterIP
