apiVersion: apps/v1
kind: Deployment
metadata:
  name: reachai-control-service
  labels:
    app: reachai-control-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reachai-control-service
  template:
    metadata:
      labels:
        app: reachai-control-service
    spec:
      containers:
        - name: reachai-control-service
          image: registry.enterprise.com/ai/reachai-control-service:latest
          ports:
            - containerPort: 18603
          env:
            - name: SERVER_PORT
              value: "18603"
            - name: AI_MYSQL_URL
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: mysql-url
            - name: AI_MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: mysql-user
            - name: AI_MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: mysql-password
            - name: REDIS_HOST
              value: redis
            - name: RUNTIME_SERVICE_URL
              value: http://reachai-runtime-service:18604
            - name: CAPABILITY_SERVICE_URL
              value: http://reachai-capability-service:18605
            - name: MODEL_SERVICE_URL
              value: http://reachai-model-service:18601
            - name: KNOWLEDGE_SERVICE_URL
              value: http://reachai-knowledge-service:18602
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1.5Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 18603
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 18603
            initialDelaySeconds: 20
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: reachai-control-service
spec:
  selector:
    app: reachai-control-service
  ports:
    - port: 18603
      targetPort: 18603
  type: ClusterIP
